import React from 'react'

const Loading=()=>{
    return(
        <div className="loading">
            {/* loading instead of section-title */}
            <h2>Loading</h2>
        </div>
    )
}

export default Loading;